apiVersion: v1
kind: Service

metadata:
  name: gunals-service

spec:
  selector:
    app: gunals-app

  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30080

  type: NodePort
